version: '3.8'

services:
  live-translator:
    build: .
    container_name: gemini-live-translator-app
    ports:
      - "3000:3000"
    environment:
      - SIGNALING_PORT=3000
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped
